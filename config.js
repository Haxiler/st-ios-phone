// ==================================================================================
// 模块: Config (静态配置)
// ==================================================================================
(function() {
    window.ST_PHONE.config = {
        // 预留的表情包库，目前为空，不影响现有逻辑
        stickers: [],
        
        // 预留的主题色配置
        theme: {
            primary: '#007AFF',
            background: '#ffffff'
        }
    };
})();
